<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white" /> <!-- Đặt màu nền là trong suốt -->
    <corners android:radius="5dp" /> <!-- Điều chỉnh bán kính của góc để làm cho nó tròn -->
    <stroke
        android:width="1dp"
        android:color="@color/white" /> <!-- Đường viền (tuỳ chọn) -->
</shape>